# 🤖 Agent 작업 가이드라인 (Strict Rules)

이 문서는 이 프로젝트에서 작업하는 모든 AI Agent가 반드시 준수해야 하는 최우선 지침입니다. 어떠한 경우에도 이 규칙을 위반해서는 안 됩니다.

## 1. 코드 보존 및 버전 관리 원칙
- **과거 코드 복구 금지**: 사용자 승인 없이 파일을 과거 시점의 코드로 덮어쓰거나 무단으로 `git checkout` 또는 `git revert`를 수행하지 않습니다.
- **상태 확인 우선**: 모든 파일 수정 전, 반드시 해당 파일의 현재 내용을 `view_file`로 확인하고 최신 로직(특히 최신 기능인 Altar 시스템 등)이 포함되어 있는지 대조합니다.
- **증분 수정**: 코드를 통째로 바꾸기보다 필요한 부분만 정확하게 수정하는 `replace_file_content` 또는 `multi_replace_file_content` 사용을 지향합니다.

## 2. 품질 및 정확성 준수
- **오탈자 및 누락 방지**: 변수명, 경로명, 오탈자가 없는지 철저히 확인합니다.
- **중복 코드 방지**: 이미 존재하는 함수나 컴포넌트를 중복 생성하지 않도록 코드베이스 검색을 생활화합니다.
- **린트 및 런타임 안전성 준수**: 
    - 작업 후 TypeScript 타입 오류가 없는지 확인합니다.
    - 특히 헬퍼 함수 호출 시 필요한 변수(예: `isDyschromatopsia` 등)가 인자로 누락되어 `ReferenceError`가 발생하지 않도록 **함수 시그니처와 호출부의 정합성**을 반드시 대조합니다.
- **엄격한 작업 범위 제어**: 
    - 사용자가 지정한 범위(예: "특정 스킬 하나만 수정")를 넘어서는 임의의 코드 수정, 리팩토링, UI 조정을 절대 금지합니다.
    - 작업 중 막히는 부분이 있다면 추측하여 수정하지 말고 즉시 사용자에게 보고합니다.

## 3. 시스템 정합성 및 빌드 규칙 존중
- **데이터-로직 동기화**: 제단 스킬과 같은 시스템 수정 시, 상수 데이터(`altarSystem.ts`)와 실제 전투 로직(`useGameLoop.ts`)의 내용이 100% 일치해야 합니다.
- **복합 시스템 규칙 준수**: 
    - "레벨당 1개 제한", "단방향 빌드(경로 의존성)" 등 시스템적으로 정의된 빌드 규칙을 코드 레벨에서 엄격히 보호해야 합니다.
    - UI 시각 효과(연결선 등)와 실제 해제 로직이 괴리되지 않도록 `AltarManager` 등의 유틸리티 로직을 정교하게 다룹니다.

## 4. 언어 및 소통 원칙
- **모든 응답 한글화**: 사용자 알림(`notify_user`), 작업 요약(`task_boundary`), 구현 계획(`implementation_plan`), 결과 보고(`walkthrough`) 등 모든 소통은 반드시 **한글**로 수행합니다.
- **문서화**: 새로운 기능을 추가할 때 관련 `md` 문서나 주석도 반드시 한글로 남깁니다.

## 5. 작업 시작 체크리스트 (Standby)
이 프로젝트의 작업을 시작할 때 Agent는 반드시 다음을 먼저 수행해야 합니다.
1. `AGENT_GUIDELINES.md`를 읽고 작업 원칙을 상기합니다.
2. 현재 `git log`를 확인하여 최신 작업 지점을 파악합니다.
3. 작업 범위를 한글로 정의하고 사용자에게 계획을 승인받습니다.
4. (중요) 단순 코드 오류를 넘어, 게임의 컨셉과 흐름에 맞는 '사용성'을 한 번 더 고민합니다.

## 6. 사용성 및 게임 흐름 재고 (Usability & Game Flow)
- **논리적 흐름 및 연속성 확인**: 새로운 기능이나 스킬 구현 시, 단순히 작동 여부를 넘어 **'저장 후 불러오기'** 시에도 상태가 유지되는지, 스테이지 전환 시 초기화가 필요한지 등 게임의 전체 가동 플로우를 재확인합니다.
- **유저 피드백의 명확성**: 유저가 현재 스킬의 On/Off 상태, 남은 사용 횟수, 효과의 적용 시점 등을 직관적으로 이해할 수 있도록 시각적(테두리, 색상) 및 텍스트적(툴팁, 문구) 피드백이 충분한지 점검합니다.
- **기획 의도와의 정합성**: 구현된 로직이 게임의 핵심 컨셉(예: 리스크와 리턴, 제한된 자원 활용 등)을 해치지 않는지, 밸런스 붕괴 요소는 없는지 재사고합니다.

---
**위 규칙을 어길 시 사용자에게 심각한 작업 손실을 초래할 수 있음을 명심하십시오.**
